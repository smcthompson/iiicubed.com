trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - '*'

pool:
  name: Default
  demands:
    - Agent.Name -equals Tydirium

variables:
  NODE_VERSION: '26.x'
  PORT: '3000'

steps:
  - task: UseNode@1
    displayName: Use Node.js $(NODE_VERSION)
    inputs:
      version: $(NODE_VERSION)

  - script: npm ci
    displayName: Install dependencies

  - script: npx playwright install --with-deps
    displayName: Install Playwright dependencies

  - script: npm run build
    displayName: 'Lint and Build'

  - script: npm run test:ci
    displayName: 'Playwright E2E Tests'
    env:
      CI: 'true'
      PORT: '3000'
      NODE_ENV: 'production'

  - task: PublishTestResults@2
    displayName: Publish Playwright test report
    condition: always()
    inputs:
      testResultsFormat: JUnit
      testResultsFiles: test-results/playwright-junit.xml
      failTaskOnFailedTests: true
      testRunTitle: Playwright E2E

  - task: PublishPipelineArtifact@1
    displayName: Publish Playwright HTML report
    condition: always()
    inputs:
      targetPath: 'playwright-report'
      artifact: 'Playwright-HTML-Report'
      publishLocation: 'pipeline'

  - task: PublishPipelineArtifact@1
    displayName: Publish test results attachments
    condition: always()
    inputs:
      targetPath: 'test-results'
      artifact: 'Playwright-Test-Results'
      publishLocation: 'pipeline'

  - task: PublishPipelineArtifact@1
    inputs:
      targetPath: 'server.log'
      artifact: 'SmokeTest-Logs'
      publishLocation: 'pipeline'
    displayName: 'Publish smoke test log'
    condition: always() # Publish even if the step fails
